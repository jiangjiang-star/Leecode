**日期**：2026-09-05  
**难度**：Easy  
**状态**：✅ 已完成

## 1. 题目理解
给定两个数组 `nums1` 和 `nums2`，返回它们的交集。  
与 349 不同，结果中**每个元素出现的次数**，应等于该元素在两个数组中出现次数的较小值。

**示例**：
- `nums1 = [1,2,2,1]`, `nums2 = [2,2]` → `[2,2]`
- `nums1 = [4,9,5]`, `nums2 = [9,4,9,8,4]` → `[4,9]`

## 2. 解题思路

### 核心直觉（一句话抓住本质）
> **先统计 nums1 的库存，再拿 nums2 去核销；匹配一个就减 1，直到库存为 0。**

### 哈希表计数法
1. 用字典 `seen` 统计 `nums1` 中每个数字出现的次数。
2. 遍历 `nums2`，如果当前数字在 `seen` 中且剩余次数 > 0：
   - 加入结果列表。
   - 将 `seen` 中该数字的计数减 1。
3. 这样保证重复元素被正确匹配，且不超过两边较少的次数。

### 与 349 的区别
| 题目 | 是否保留重复 | 核心工具 |
|------|------------|---------|
| 349 交集 | 否，去重 | `set` 集合 |
| 350 交集 II | 是，按较小次数保留 | `dict` 计数 |

## 3. 代码

​```python
class Solution(object):
    def intersect(self, nums1, nums2):
        seen = {}
        for num in nums1:
            seen[num] = seen.get(num, 0) + 1   # 统计 nums1 的次数
        
        result = []
        for num in nums2:
            if num in seen and seen[num] > 0:  # 有库存才匹配
                result.append(num)
                seen[num] -= 1                 # 用掉一个
        
        return result
​```

## 4. 复杂度分析
- **时间复杂度**：O(n + m)，分别遍历两个数组。
- **空间复杂度**：O(min(n, m))，字典存较短数组的计数。

## 5. 踩坑记录
- `seen.get(num) + 1` 报错 → 第一次出现时 `get` 返回 `None`，应该用 `seen.get(num, 0) + 1`。
- `seen.get != 0` 永远为 True → `seen.get` 是方法对象，不是值。应该用 `seen[num] > 0`。
- 忘记 `seen[num] -= 1`，导致重复元素被过度匹配。
- 与 349 混淆：349 用集合去重，350 需要字典计数保留重复。

## 6. 一句话总结
**“字典计数 + 核销减一，是处理带重复交集的正确姿势。”**