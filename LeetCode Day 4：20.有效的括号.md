**日期**：2026-08-09  
**难度**：Easy  
**状态**：✅ 已完成
## 1. 题目理解
给定一个只包含 `()`、`[]`、`{}` 的字符串，判断括号是否全部正确配对。  
- `()` `()[]{}` `{[]}` → 正确  
- `(]` `([)]` `[` → 错误

## 2. 解题思路

### 核心直觉
> **左括号直接入栈，遇到右括号就进行匹配。栈空或栈顶不匹配则失败，最后栈必须为空。**

### 算法步骤
1. 创建空栈 `stack = []`
2. 用字典 `pairs` 定义每个右括号对应的左括号
3. 遍历字符串每个字符 `ch`：
   - 如果 `ch` 是右括号：
     - 栈为空 → 失败（右括号多余）
     - 弹出栈顶，和 `pairs[ch]` 比较，不匹配 → 失败
   - 如果 `ch` 是左括号：
     - 直接压入栈
4. 遍历结束，返回 `not stack`（栈空才是完全匹配）

### 为什么用栈？
- 括号匹配是“最近配对”问题，栈的后进先出完美模拟这一结构。
- 计数法无法处理 `([)]` 这种嵌套错误，栈可以。

## 3. 代码

​```python
class Solution(object):
    def isValid(self, s):
        stack = []
        pairs = {')': '(', ']': '[', '}': '{'}
        for ch in s:
            if ch in pairs:   # 遇到右括号
                if not stack or stack.pop() != pairs[ch]:
                    return False
            else:             # 左括号入栈
                stack.append(ch)
        return not stack
​```

## 4. 复杂度分析
- 时间复杂度：O(n)，每个字符入栈/出栈一次
- 空间复杂度：O(n)，最坏情况全是左括号，栈存满整个字符串

## 5. 踩坑记录
- **拼写错误**：`&&` 是 C/Java 语法，Python 用 `and`
- **变量名不统一**：`stcak` vs `stack`，导致 `NameError`
- **提前 `return False`**：在判断右括号时多写了一个无条件 `return False`，导致所有右括号直接失败
- **栈空时 `pop()`**：必须先检查 `not stack`，否则空栈 `pop()` 抛异常
- **`for i in len(s)`**：`len()` 返回整数不能迭代，应该用 `for ch in s` 或 `for i in range(len(s))`

## 6. 总结
左括号入栈，右括号匹配栈顶，不匹配或栈空则失败，最后栈空才成功。